<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wisedu.scc.love" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:name=".application.LoveApplication"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.SplashActivity_"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity_"></activity>
        <activity android:name=".activity.RegisterActivity_"></activity>
        <activity android:name=".activity.MainActivity_"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar"
            ></activity>

        <!--暂时未用到 begin-->
        <activity android:name=".activity.image.ImageCutActivity_"></activity>、
        <activity android:name=".activity.image.ImageShowActivity_"></activity>
        <!--暂时未用到 end-->

    </application>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>
